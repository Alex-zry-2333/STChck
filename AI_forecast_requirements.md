AI 预报界面 需求说明书

版本: 2026-07-25
作者: Copilot CLI (代理)

概述
----
本文件整理并规范化“设备运行状态 AI 预报”功能的完整需求，覆盖产品/运维/前端/后端/安全/测试等方面，供开发、产品和运维参考。目标是为气象站运维和预报员提供可解释、可操作的智能风险预判与优先巡检建议。

一、目标用户与使用场景
-------------------
- 目标用户：气象站运维人员、产品经理、预报员、值班工程师。
- 使用场景：
  - 日常值班时快速浏览全网风险分布并决定巡检优先级；
  - 巡检前查看某站点详细风险因子与运维建议；
  - 运行异常时快速识别多点故障或通信链路问题；
  - 产品评审时展示 AI 预报如何辅助决策。

二、核心功能（高优先级）
---------------------
1. 列表视图（/forecast）
   - 风险等级统计（高/中/低）卡片：显示数量与简要说明。
   - 风险等级饼图（可视化分布）。
   - 筛选栏：按站点名称/ID 搜索，按风险等级筛选。支持快速刷新。
   - 站点风险卡片列表：每个卡片包含站点名、风险等级标签、摘要、关键建议、置信度分数、查看详情按钮。
   - 卡片按风险等级、置信度或告警数排序（可扩展）。

2. 详情面板（侧边滑出）
   - 打开方式：点击卡片的“查看详情”按钮或从其它页面跳转。
   - 内容结构：
     - 标题/副标题：站点名、风险等级、置信度、生成时间
     - 风险聚焦：摘要、重点提示、预测状态
     - 风险因子（可解释性条目）：在线/离线、24h 到达率、告警数、最近到达时间等
     - 关键触发点（列表）：列出影响判断的主要告警或异常项
     - 运维建议（列表）：优先巡检提示、检查顺序、排查要点
     - 操作按钮：复制建议、生成巡检任务（后续对接工单系统）、跳转到站点设备详情
   - 可访问性：键盘可关闭（Esc）、Overlay 点击关闭；ARIA 属性；移动端自适应宽度。

3. 后端 API
   - GET /api/forecast
     - 描述：返回所有站点的预报概览
     - Query: optional station (string) 用于按站点过滤
     - 返回: JSON array of ForecastOverview
       - ForecastOverview 字段：station_id, station_name, risk_level (高/中/低), risk_score (数值/百分比), summary, highlight, advice, updated_at
   - GET /api/forecast/{id}
     - 描述：返回单站点详细预报
     - 返回: ForecastDetail
       - ForecastDetail 字段：station_id, station_name, risk_level, risk_score, summary, highlight, predicted_state, risk_factors (array), key_triggers (array), maintenance_advice (array), confidence, generated_at
   - 性能：支持 1000 次/分钟 的读请求（水平可扩展）；响应时间 <200ms（模拟数据）

4. 风险计算（后端逻辑）
   - 输入数据：StationStatus（来自 /api/status 或 DB）包含在线、alarm_count、arrival_rate_24h、last_arrival_time 等
   - 评分规则（当前规则为可解释规则引擎）：
     - 离线：基线高分（例如 +0.7）
     - 告警数：每条告警累加（例如每条 +0.12）
     - 到达率：若 <95%，按差值线性加分（例如 (95 - rate) * 0.015）
     - 最近到达时间差 >120min 加分 +0.15
     - 最终 clamp(0,1)，映射为百分比与等级： >=0.65 高 / >=0.35 中 / else 低
   - 输出：risk_score (0~1 float), risk_level, 可解释性条目（为什么得出该分数）

5. 可解释性与可操作建议
   - 每个 ForecastDetail 包含 risk_factors（用于说明得分组成）和 maintenance_advice（可拷贝的运维步骤）。
   - 建议要短且可执行，例如“优先检查供电->网络->温度模块”。

三、界面交互细节（产品经理 & 预报员视角）
-----------------------------------
1. 默认排序：高风险优先；相同等级按 risk_score 降序。
2. 卡片设计要突出“为什么优先”——在卡片上显示一条触发理由（例如“离线 / 多点告警 / 到达率低”）。
3. 详情面板为运维决策页面：
   - 在顶部显示即时操作按钮（复制建议 / 生成工单 / 跳转设备详情）。
   - 风险因子按重要性排序（离线 > 告警数 > 到报率 > 最近数据时间）。
   - 支持“展开更多历史”按钮（后续接入趋势图）。
4. 可视化要支持印刷/导出（后续需求）：将 detail 导出为文本或 PDF。

四、权限与安全
---------------
1. 页面和 API 受认证保护（现有基于 session_token 的登录）。
2. 仅允许有运维/值班角色的账号访问预报详情（后续可扩展 RBAC）。
3. 防止暴力破解：登录接口建议接入限流和账户锁定策略（如连续 5 次失败锁定 15 分钟）。
4. API 速率限制：按 IP 与 API key（后续）做读流控，保护后端数据库。
5. 日志与审计：记录谁查看了哪个站点的预报（便于事后溯源）。

五、前端实现注意事项
------------------
1. 兼容性：现代浏览器（Chromium-based、Firefox、Safari）及移动端自适应。
2. 性能：分页或虚拟列表（如站点较多时），避免一次性渲染成千上万 DOM 节点。
3. 可访问性：ARIA、键盘操作、语义化标签。
4. 交互流畅：侧边面板动画、loading 状态、错误提示。
5. 国际化（后续）：界面文本建议走 i18n 通道（中文为主）。

六、测试要求
-------------
1. 单元测试：
   - 后端风险计算函数（calculate_risk_score）的边界条件测试（在线/离线、高告警/无告警、到达率阈值等）。
   - API 返回结构与字段完整性测试。
2. 集成测试：
   - 从模拟数据生成预报并验证前端渲染逻辑。
3. E2E：
   - 登录 -> 打开 /forecast -> 筛选 -> 打开详情 -> 复制建议/生成工单（若接入）
4. 可用性测试：与真实运维人员做 1-2 次可用性验证，收集优先巡检建议是否符合运维直觉。

七、监控与度量
---------------
1. API 指标：请求次数、平均响应时间、错误率。
2. 业务指标：日活跃查看预报的运维人数、通过预报触发的工单数、预报命中率（后续评估）。
3. 监控告警：当 /api/forecast 调用失败率 > 2% 时告警。

八、交付与里程碑（建议）
---------------------
1. M0（1d）：完成后端 forecast API 基本实现 + 前端列表页与侧边详情面板样式。
2. M1（2d）：完善可解释性字段、增加“生成工单”占位 API、完成单元测试。
3. M2（3d）：性能优化、可用性评审、README/文档更新、推送到测试环境。

九、兼容与回退策略
-----------------
- 如果风险引擎出现异常，允许降级为“仅显示告警数和在线率”的简单视图，保持可用性。

十、交付清单（文件/接口/改动）
-------------------------
- 新增/修改前端模板： templates/forecast.html（详情面板）、templates/overview.html（入口卡片）、templates/dashboard.html（入口卡片）
- 后端： src/monitor.rs（generate_forecast_overview, generate_forecast_detail, calculate_risk_score）、src/models.rs（ForecastDetail 字段扩展）、src/main.rs（路由 /api/forecast）
- 文档： README.md 增加改版履历与 API 说明

附：示例 API 返回（概览）
GET /api/forecast
[
  {
    "station_id": "50936",
    "station_name": "吉林白城",
    "risk_level": "高",
    "risk_score": 84.0,
    "summary": "当前告警集中且数据到达率下降，建议优先现场检查",
    "highlight": "多点异常告警",
    "advice": "现场巡检机箱温度、蓄电池电压及通信网口",
    "updated_at": "2026-07-25 09:00:00"
  }
]

附：示例 API 返回（详情）
GET /api/forecast/50936
{
  "station_id": "50936",
  "station_name": "吉林白城 (50936)",
  "risk_level": "高",
  "risk_score": 84.0,
  "summary": "当前告警集中且数据到达率下降，建议优先现场检查",
  "highlight": "多点异常告警",
  "predicted_state": "警戒风险: 当前运行状态存在多点异常",
  "risk_factors": [
    "当前在线: 设备正在上报数据",
    "告警数量: 4 条",
    "24h 到达率: 82.3%",
    "最近到达时间: 2026-07-25 06:12:00"
  ],
  "key_triggers": ["xB: 蓄电池电压偏低", "tD: 网口通信异常"],
  "maintenance_advice": ["优先排查供电与网络链路", "检查机箱门、蓄电池电压、智能电源状态"],
  "confidence": "高",
  "generated_at": "2026-07-25 09:00:00"
}

-- 结束 --
